/*
import {renderThumbnail} from './thumbnail.js';

const bigPicture = document.querySelector('.big-picture__img');
const likes = bigPicture.querySelector('.likes-count');
const visibilityComment = bigPicture.querySelector('.social__comment-shown-count');
const comment = bigPicture.querySelector('.social__comment-total-count');

const modalPicture = renderThumbnail();
modalPicture.forEach = (url) => {
  src = url;

};


const modalPicture = renderThumbnail();

modalPicture.forEach(({url}) => {
  const bigPicture = document.querySelector('.big-picture__img');
});
*/
